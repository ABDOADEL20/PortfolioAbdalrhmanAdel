@model List<UserInRoleVm>
@{
    Layout = null;
    var RoleId = ViewBag.RoleId;
}

<form asp-action="AddOrRemoveUsers" asp-controller="Roles" asp-area="Dashboards" asp-route-RoleId="@RoleId" method="post" class="form-group">
    <ul class="list-group">
        @for (int i = 0; i < Model.Count; i++)
        {
        <li class="list-group-item">
         <input asp-for="@Model[i].IsSelected" type="checkbox" class="filled-in">
        <label asp-for="@Model[i].IsSelected">@Model[i].UserName</label>

        <input type="hidden" asp-for="@Model[i].UserId" />
        <input type="hidden" asp-for="@Model[i].UserName" />
        </li>
        }
    </ul>

    <div class="card-footer">
        <input type="submit" value="Edit" class="btn btn-primary" />
    </div>
</form>

